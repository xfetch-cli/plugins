[package]
name = "xfetch-plugin-github-stats"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that shows GitHub statistics via the GitHub API"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
