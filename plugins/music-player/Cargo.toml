[package]
name = "xfetch-plugin-music-player"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that shows the current track via playerctl/mpd/Spotify probes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
