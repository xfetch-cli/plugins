[package]
name = "xfetch-plugin-animate-logo"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that animates the logo with user-controlled frames and duration"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.148"
xfetch-plugin-api = { workspace = true }
