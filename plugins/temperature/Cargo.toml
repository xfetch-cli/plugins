[package]
name = "xfetch-plugin-temperature"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that shows CPU temperature from hwmon/sensors probes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
