[package]
name = "xfetch-plugin-display-resolution"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that reports the display resolution via xrandr/wayland probes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
