[package]
name = "xfetch-plugin-weather"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that shows the current weather from wttr.in"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
