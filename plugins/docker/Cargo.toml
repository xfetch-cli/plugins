[package]
name = "xfetch-plugin-docker"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that reports Docker status and containers from the local daemon"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
