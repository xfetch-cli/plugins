# Changelog

## 2026-08-19
- Wrapped work in `with_timeout` with a 20 s budget; on timeout it responds with a fallback line.
- Default registry moved to the new `xfetch-cli/themes` repository (was `xfetch-cli/configs/themes`).
