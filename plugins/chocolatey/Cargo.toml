[package]
name = "xfetch-plugin-chocolatey"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that counts locally installed Chocolatey packages"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
