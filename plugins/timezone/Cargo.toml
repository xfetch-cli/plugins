[package]
name = "xfetch-plugin-timezone"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Xfetch plugin that shows the current timezone via local probes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
xfetch-plugin-api = { workspace = true }
